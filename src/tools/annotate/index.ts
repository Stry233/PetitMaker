/**
 * The door: the plan-notes tool as callers outside `tools/` take it. Modules inside `tools/`
 * import files directly; everything else comes through here.
 */
export { AnnotateTool } from './annotate-tool';
